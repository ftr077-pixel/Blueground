node_modules/
.next/
out/
build/
.DS_Store
*.log
.env*.local
.vercel
next-env.d.ts
*.tsbuildinfo
data/

# python (scraper)
__pycache__/
*.pyc
.venv/
scraper/.venv/

# Secrets — never commit. Covers editor backups (.env.save, .env~) and
# per-environment copies (.env.local) as well as the file itself.
# .env.example is the blank template and stays tracked.
.env
.env.*
.env~
!.env.example

# PriceLabs PDF drop folder (operator's downloaded reports, not source)
scraper/pricelabs-inbox/

# PriceLabs browser automation: saved session + capture/download artifacts (secrets/data)
scraper/.pricelabs_state.json
scraper/pricelabs-captures/
scraper/pricelabs-downloads/

# Call transcripts live here. Never in git.
voicebridge/data/
